STATUS: exploring
APPROACH: Derive PV B-operand mapping after Triton lane-mix (no extra post-mix), then apply a precise lane/byte permutation to restore identity-P (row/col).
KEY_PARAMS: P packed via `v_cvt_scalef32_pk_fp8_f32` into v48-v55; B operand mix uses Triton lane-mix (v237 = (lane<<2)^0x80, vcc = lane<32); V uses `ds_read_b64` (no TR8) as A operand; MFMA uses v[0:7] (V) x v[48:55] (P).
TEST_RESULT:
  - Build: PASS
  - Scaffold numerics (test_scaffold_numerics.py):
    * NUMERICS_IDENTITY_P=1 NUMERICS_V_PATTERN=col: FAIL (columns repeat in 8-wide groups)
    * NUMERICS_IDENTITY_P=1 NUMERICS_V_PATTERN=rowid: FAIL (row>>1; rows duplicated)
NEXT_STEP: Analyze `p_pack_mapping.csv` + layout probes to derive exact B-operand permutation (lane/byte) that restores identity-P.
